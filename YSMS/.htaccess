Options -Indexes

# Note: the Apache-behind-Cloud-Run trailing-slash/port fix lives in the root
# .htaccess only — it already covers every top-level directory (including
# this one). A second copy here caused a conflicting nested mod_rewrite rule.

php_flag display_errors Off
php_flag log_errors On
php_value error_reporting 32767
